spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

logging:
  level:
    com.auth: INFO
    org.springframework.security: WARN
    org.springframework.web: WARN

app:
  sms:
    enabled: false